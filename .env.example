GMAIL_USER=
TZ=
LOOKBACK_DAYS=2
MAX_THREADS_PER_RUN=50

# simulate | openai_compatible
LLM_MODE=simulate
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4.1-mini

CONFIDENCE_THRESHOLD=0.55
SEND_DIGEST=true

# customize email classification domains (minimum 2)
# --- Domain set: Balanced ---
DOMAINS=audit,deadlines,followups,receivables,meetings,needs_response,noise,other
BUCKET_LABELS=audit:Audit Requests & Evidence,deadlines:Deadlines & Expiries,followups:Follow-ups,receivables:Payments & Receivables,meetings:Meetings,needs_response:Needs Response,noise:Noise,other:Other
DOMAIN_DEFAULT=other

# # --- Domain set: Audit-heavy ---
# DOMAINS=evidence_requests,open_questions,deadlines,client_followups,approvals,meetings,noise,other
# BUCKET_LABELS=evidence_requests:Evidence Requests,open_questions:Open Questions / Clarifications,deadlines:Deadlines & Expiries,client_followups:Client Follow-ups,approvals:Approvals / Sign-off,meetings:Meetings,noise:Noise,other:Other
# DOMAIN_DEFAULT=other

# # --- Domain set: Cashflow / AR ---
# DOMAINS=invoices_due,payment_promises,blockers,disputes,client_followups,meetings,noise,other
# BUCKET_LABELS=invoices_due:Invoices Due,payment_promises:Payment Promises,blockers:Payment Blockers,disputes:Disputes / Short-pays,client_followups:Client Follow-ups,meetings:Meetings,noise:Noise,other:Other
# DOMAIN_DEFAULT=other